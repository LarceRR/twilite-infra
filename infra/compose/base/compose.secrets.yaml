# Template only. Never commit populated secret files.
# DB_ADMIN_SECRET_FILE, REDIS_ACL_FILE and environment-specific URL/token file
# variables must point to root-only material recovered from OpenBao or backups.
