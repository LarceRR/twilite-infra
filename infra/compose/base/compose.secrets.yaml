# This file is a template only. Never commit populated secret files.
# The deployment command supplies POSTGRES_SUPERUSER_PASSWORD_FILE and Redis ACL material
# from OpenBao or the operator's local secret recovery directory.
